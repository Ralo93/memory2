## First phase

I need a simple chat-bot which keeps a conversation summary inside the context-window.
  
I need a gui which shows the full context window of each call to the LLM, depending on the size of it.
  

I want a simple implementation of an episodic LTM, using an Agent which creates entries into the database and a simple semantic retriever.


